// public 方法
util.goToPage()

// private 方法
util.goToPage()

util.goToPage()

/**
 * @param {string} name
 * @param {number} age sadf
 * @returns {string} 欢迎语
 */
function greet(name, age = 18) {
  console.log(name)
  return 'halo' + name
}